# 宫干自化（T181）

> 权威说明；实现见 `tables/zihua.ts`、`zihua.ts`。**禁止 LLM 补全。**

## 1. 范围

- 在 **生年四化** 之后，用 **宫干** 查同一套四化星名表。
- 若目标星落在 **本宫**，则追加 `自化禄|自化权|自化科|自化忌`。
- **不是**完整飞星派：不改 `meta.school`（仍 `sanhe`）；不实现飞宫迭化全套。
- 与生年四化可叠写于 `star.sihua`（如 `["禄","自化忌"]`）。

## 2. 表源

与 `SIHUA_BY_YEAR_STEM` 同源（`tables/sihua.ts`），避免双表漂移。

## 3. 禁止

1. 禁止 silently 混入飞星派飞宫规则。
2. 禁止 LLM 推断自化。
3. 飞星飞宫边表见 `feixing.md`（T190；与本文件「本宫自化叠标」互补）。

## 4. 版本

自化 + 博士入盘：`0.4.0`；飞星飞宫 + 流昌截空：`0.5.0`。
