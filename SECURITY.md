# 安全策略

## 支持范围

安全修复优先应用于默认分支和最新发布版本。未发布开发分支不提供长期安全维护。

## 报告漏洞

请使用 GitHub 仓库的 Private Vulnerability Reporting / Security Advisory 报告：

- 账号接管、Magic Link 或 Session 绕过；
- 跨用户读取、修改或删除命盘；
- API Key、Token、邮箱、生辰或数据库连接串泄露；
- SSRF、XSS、CSRF、注入或任意代码执行；
- Docker 镜像中的密钥或不安全权限；
- 可绕过的限流和高成本 LLM 滥用。

不要创建公开 Issue，也不要附带真实用户数据。报告中请提供影响范围、复现步骤、脱敏请求和已知利用条件。

## 响应目标

- 2 个工作日内确认收到；
- 7 个工作日内给出初步分级和处理计划；
- 高危问题优先修复，并协调披露时间。

## 密钥泄露

如怀疑密钥泄露，应立即轮换凭证、撤销 Session、检查日志，并确认泄露值未进入 Git 历史。
